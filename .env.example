# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Google GenAI (for visual + review analyzers)
GOOGLE_API_KEY=

# NVIDIA NIM (optional — enables NIM models for specific analyzers)
NVIDIA_API_KEY=

# LLM Provider: "auto" (default), "gemini", "nvidia"
LLM_PROVIDER=auto

# Backend URL (used by the extension)
DARKGUARD_API_URL=http://localhost:8000/api/analyze

# Analyzer timeouts (seconds)
ANALYZER_TIMEOUT=10

# CORS (configured in settings.py)
# Allowed: chrome-extension://* , localhost:8000, localhost:3000
